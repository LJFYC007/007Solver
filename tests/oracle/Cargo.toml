[package]
name = "solver-test-oracle"
version = "0.1.0"
edition = "2021"

[dependencies]
postflop-solver = { git = "https://github.com/b-inary/postflop-solver", rev = "9d1509fe5077d019825f833eed04b16d342dfda1", default-features = false, features = ["rayon"] }
# Preserve the fixture's f64 weights when copying scenario provenance to references.
serde_json = { version = "1.0", features = ["float_roundtrip"] }
