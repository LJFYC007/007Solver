{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Default permissions for the solver",
    "windows": ["main"],
    "permissions": ["core:default"]
}
